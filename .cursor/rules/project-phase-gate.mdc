---
description: Knowledge 项目阶段门禁与阶段验收规则
alwaysApply: true
---

# Knowledge 项目阶段门禁

- 项目按 Milestone 分阶段推进：必须先完成当前阶段，再进入下一阶段。
- 当前 Milestone 未达到 `100%` 前，不得继续执行下一 Milestone 的新任务。
- Milestone 完成要求：该阶段所有 Parent Issue 和 Sub-issue 均为 `Done`，或经用户确认进入 `Canceled` / `Duplicate` 等终态。
- 如果下一阶段已有提前完成任务，不回退历史，但暂停继续推进，先补齐当前阶段。
- 用户批准当前 Milestone 启动后，阶段内单个任务完成并通过验证时，可以直接提交、推送、创建 PR、合并 PR 并同步 Linear；不再逐任务请求确认。
- 只有进入新 Milestone、需要用户配置/账号/密钥/费用/隐私授权、改变架构或扩大阶段范围、阶段验收失败时，才必须暂停等待用户确认。
- 每个 Milestone 完成后，必须向用户交付阶段说明：完成了哪些任务、达到了什么可观察效果、如何通过前端验证实现功能。
- 阶段完成后的前端验证必须包含入口、前置条件、操作步骤和预期结果。若当前阶段没有用户可见前端功能，必须说明原因，并提供最接近用户视角的替代验证方式。